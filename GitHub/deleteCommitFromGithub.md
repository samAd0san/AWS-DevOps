### In this file we'll learn how to delete commit history from git repository. 

```
git log --oneline
git reset --hard HEAD~1
git push origin main --force
```

- In this example, we deleted one commit by 'HEAD~1' from the github repository
